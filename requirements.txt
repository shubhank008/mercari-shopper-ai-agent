#Python 3.10+

# Official LLM SDKs
anthropic>=0.25.0
# (Optional) For fallback, disable in .env if not required
openai>=1.20.0

# Data Models
pydantic>=2.7.0                     # Data Validation
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# Web Scraping / API Requests
#httpx>=0.27.0
httpx<0.28.0
beautifulsoup4>=4.12.3
ecdsa>=0.19.0

# Python-Jose required for DDoP-JWT key signing
# https://github.com/mpdavis/python-jose
python-jose[cryptography]>=3.3

# UI & Utilities
rich>=13.7.1                        # Rich text formatting
tenacity>=8.3.0                     # For Retries

# (Optional) For fallback, disable in .env too if not required
playwright>=1.43.0

# (Optional) For fallback, disable in .env too if not required
mercapi>=0.4.2

# Web demo and ASGI deployment
fastapi==0.115.12
uvicorn[standard]==0.34.2
